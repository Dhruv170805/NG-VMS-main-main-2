import { visitorTypeDefs } from './typeDefs/visitor';
import { visitorResolvers } from './resolvers/visitor.resolver';

export const typeDefs = [visitorTypeDefs];
export const resolvers = [visitorResolvers];
